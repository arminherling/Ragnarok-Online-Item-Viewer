<UserControl x:Class="RagnarokOnlineItemViewer.Views.ItemDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel Margin="10" Background="Beige">
        <DockPanel DockPanel.Dock="Top">
            <TextBlock Margin="10"  Text="ID:"/>
            <TextBox Margin="0,10,0,7" Text="{Binding ID, Mode=OneWay}" MinWidth="40" IsReadOnly="True"/>
            <TextBlock Margin="10" Text="Name:"/>
            <TextBox Margin="0,10,10,7" Text="{Binding Name, Mode=OneWay}" IsReadOnly="True"/>
        </DockPanel>
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Margin="10,0,0,7" Text="Description:"/>
            <TextBox Margin="10,0,10,10" Text="{Binding Description, Mode=OneWay}" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True"/>
        </DockPanel>
    </DockPanel>
</UserControl>
